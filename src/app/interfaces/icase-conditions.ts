export interface ICaseConditions {
  postalcode: string;
  postalcode_description: string;
  status: number;
  status_text: string;
  summary_order: number;
  hits: string;
  latest_status_ts: string;
}
